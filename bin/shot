#!/bin/sh
#
# shot

case "$1" in
    0x*) wattr "$1" && wid="$1" || exit 1 ;;
    *)   wid="$(lsw -r)"                  ;;
esac

if [ -z "$1" ]; then
    file="$1"
else
    file="$HOME/$(date +%d-%m-%y-%H:%M:%S)"
fi

xscreenshot "$wid" | ff2png > "$file.png"
