#!/bin/sh
#
# shot

case "$1" in
    0x*)
        wattr "$1" && wid="$1" || exit 1
        file="$(atomx WM_CLASS "$wid")-$wid-$(date +%s)"
        ;;
    *)
        wid="$(lsw -r)"
        file="$HOME/$(date +%s)"
        ;;
esac

xscreenshot "$wid" | ff2png > "$file.png"
