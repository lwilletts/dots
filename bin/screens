#!/bin/sh
#
# screens

main() {
    . fwmrc
    wmgaps

    LAYOUTDIR="$HOME/.screenlayout"

    case "$1" in
        pri)
            "$LAYOUTDIR/pri.sh"

            for wid in $(lsw); do
                move center "$wid"
            done
            ;;
        *)
            case "$(hostname)" in
                "fyriousa")
                    "$LAYOUTDIR/default.sh"
                    ;;
            esac
            ;;
    esac
}

main "$@"
