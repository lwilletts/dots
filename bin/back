#!/bin/sh
#
# back

usage() {
    base=$(basename "$0")

    cat >&2 << EOF
Usage:
    $base
EOF

    [ $# -eq 0 ] || exit "$1"
}

main() {
    hsetroot -fill "$HOME/tmp/mono/rock.jpg" -brightness -0.01
}

main "$@"
