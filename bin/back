#!/bin/sh
#
# back

usage() {
    base=$(basename "$0")

    cat >&2 << EOF
Usage:
    $base
EOF

    [ $# -eq 0 ] || exit "$1"
}

main() {
    hsetroot -root -tile "/home/fyr/usr/img/patterns/noisy_net.png"
}

main "$@"
